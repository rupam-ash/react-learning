const Form = () =>{
return (
    
    <div>
        <form onClick={}>
        <input type="text" placeholder="Add yout todos ..."/>
        <button>Add</button>
        </form>
    </div>
 
)
}
export default Form